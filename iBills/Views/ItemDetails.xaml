<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="iBills.Views.ItemDetails"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    Title="{Binding Title}"
    prism:ViewModelLocator.AutowireViewModel="True">

    <StackLayout>
        <Label FontSize="Medium" Text="Text" />
        <Entry FontSize="Small" Text="{Binding SelectedItemText}" />
        <Label FontSize="Medium" Text="Description" />
        <Editor
            Margin="0"
            FontSize="Small"
            Text="{Binding SelectedItemDescription}" />


        <Button Command="{Binding GoToMainCommand}" Text="Go to main" />
    </StackLayout>
</ContentPage>